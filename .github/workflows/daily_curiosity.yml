name: Daily Curiosity (New Channel)


on:
  schedule:
    # Run at 12:30 AM IST (2:00 PM EST) - Afternoon Peak
    - cron: '0 19 1-31/2 * *'→ Runs at 19:00

  workflow_dispatch:


permissions:
  contents: write


jobs:
  build-and-post:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3


      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'


      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg imagemagick ghostscript
          # Fix ImageMagick policy
          if [ -f /etc/ImageMagick-6/policy.xml ]; then
            sudo sed -i 's/none/read,write/g' /etc/ImageMagick-6/policy.xml
          fi
          pip install -r requirements.txt


      - name: Generate and Upload Video
        env:
          # Use NEW CHANNEL Secrets mapped to standard Env Vars
